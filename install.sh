#!/bin/sh
# Pre-built binary download has been removed for security.
# To install, build from source:
#   git clone https://github.com/nethunterocean-cmyk/unity-cli
#   cd unity-cli
#   go build -o unity-cli .
#   sudo mv unity-cli /usr/local/bin/
echo "Download install is disabled for security. Build from source instead."
echo "See README.md for build instructions."
exit 1
